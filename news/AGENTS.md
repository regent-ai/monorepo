# News

This folder contains draft posts (Markdown) that get published to Paragraph and surfaced via `news.regent.cx` redirects.

- Keep changes scoped to `monorepo/news/` unless explicitly asked to cross-link into `monorepo/documentation/`.
- Prefer factual, technical writing; label speculation clearly.