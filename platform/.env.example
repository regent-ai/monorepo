# Regent Platform (.env template)
# Copy this file to `.env` (or rename it to `.env.example`) and fill in values.
# Do NOT commit `.env`.
#
# Important: Vite exposes variables that start with `VITE_` and `BASE_` to the browser
# (see `vite.config.ts` `envPrefix`). Treat those as PUBLIC configuration.

# ------------------------------------------------------------------------------
# Public (client-exposed) config
# ------------------------------------------------------------------------------

# Base Mainnet RPC URL (chainId 8453). Required.
# Used by:
# - viem public client in `/redeem` (`src/components/redeem/redeem-widget.tsx`)
# - wallet network add/switch (`src/components/wallet/wallet-connector.tsx`)
# Must be set explicitly; there is NO hardcoded fallback.
BASE_RPC_URL=

# Public base URL for the app (used for links and client-side URL construction).
# Defaults to `http://localhost:3000` if omitted.
VITE_BASE_URL=http://localhost:3000

# ERC-8004 subgraph endpoint used by the Explorer and Agent pages.
# Leave blank to use the in-code default, or set to your own hosted subgraph URL.
VITE_ERC8004_SUBGRAPH_URL=

# Redeemer contract address (AnimataRedeemer) on Base mainnet.
# Required to enable on-chain redemption calls in `/redeem`.
VITE_NEXT_PUBLIC_REDEEMER_ADDRESS=

# Fleet backend WebSocket base URL (optional).
# If set, the ops UI will open realtime subscriptions (e.g. wss://fleet-api.regent.cx).
VITE_FLEET_WS_BASE_URL=

# ------------------------------------------------------------------------------
# Server-only config (NOT exposed to the browser)
# ------------------------------------------------------------------------------

# Postgres connection string for Drizzle (optional unless you enable DB features).
# Example: postgres://user:pass@localhost:5432/regent
DATABASE_URL=

# OpenSea API key used by the server proxy for holdings lookup on `/redeem` (optional).
# If missing, users can still redeem by manually entering a token id; holdings list will be unavailable.
OPENSEA_API_KEY=

# Fleet ops backend HTTP base URL (optional).
# Enables `/dashboard` and `/agent/:id?tab=ops` data via the platform proxy routes under `/api/fleet/*`.
FLEET_API_BASE_URL=

# Optional admin token forwarded by admin-only proxy endpoints under `/api/fleet/*`.
# Only needed if you want the platform to call ensure/chat send admin actions.
FLEET_ADMIN_TOKEN=


